// JSON config for this frontend project
export const config = {
    // identifier used for localStorage
    appId: "musicStorage",

    apiEndpoint: "http://localhost:64194/api/",

    genre: ["Pop", "Rock", "Dance", "Latin"]
};
